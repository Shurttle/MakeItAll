<Window x:Class="MakeItAll.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MakeItAll"
        mc:Ignorable="d"
        Title="RegisterWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Логин:"/>
        <TextBox x:Name="txtUsername" Grid.Row="0" Margin="0,20,0,0"/>

        <TextBlock Text="Пароль:" Grid.Row="1" Margin="0,20,0,0"/>
        <PasswordBox x:Name="txtPassword" Grid.Row="1" Margin="0,40,0,0"/>

        <TextBlock Text="Email:" Grid.Row="2" Margin="0,60,0,0"/>
        <TextBox x:Name="txtEmail" Grid.Row="2" Margin="0,80,0,0"/>

        <Button Content="Зарегистрироваться" Grid.Row="3" Height="25" Click="Register_Click" Margin="0,120,0,0"/>
    </Grid>
</Window>